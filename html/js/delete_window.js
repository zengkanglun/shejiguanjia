

$(document).on("click", "#boxPock .del .btn2,#boxPock .del_head i", function() {
		$("#boxPock").hide();
		$("#boxPock .del").hide();
	})

$(document).on("click", "#boxPock .cover .btn2,#boxPock .cover_head i", function() {
		$("#boxPock").hide();
		$("#boxPock .cover").hide();
	})